<ul class="nav navbar-nav">
  <% Godmin.resources.each do |resource| %>
    <% if current_page?(controller: resource) %>
      <li class="active">
    <% else %>
      <li>
    <% end %>
      <%= link_to resource.to_s.titleize, resource %>
    </li>
  <% end %>
</ul>
